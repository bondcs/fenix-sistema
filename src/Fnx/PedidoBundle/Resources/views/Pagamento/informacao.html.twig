<div style="text-align: right; float: right; margin-top:3px; margin-right: 1% ">
    <a href="{{path('PagamentoPedidoRemove', {'id' : pedido.id})}}" class="confirm-link dialog-confirm-link" ><img src="{{asset("bundles/fnxadmin/images/delete-money.png")}}" alt="Excluir Pagamento"></a>
    </div>
<div class="ui-panel-content ui-widget-content " style="width:99.1%">
    <h4 class="ui-panel-title ui-widget-header">Registro</h4>
    <table class="infos">
                <tr>
                    <th>Data:</th>
                    <td>{{pedido.registro.data|date('d/m/Y H:m:i')}}</td>
                </tr>
                <tr>
                    <th>Descrição:</th>
                    <td>{{pedido.registro.descricao}}</td>
                </tr>
                <tr>
                    <th>Doc:</th>
                    <td>{{pedido.registro.id}}</td>
                </tr>
    </table>
</div>
